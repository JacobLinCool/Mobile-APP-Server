# Mobile APP Server
 Mobile APP Server

## API Endpoints

### POST /api/create

return `{ "id": "abc123" }`

### GET /api/get?id=`id`

return `Challenge`

### GET /api/all

return `{ id: Challenge }`
